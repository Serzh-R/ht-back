export type BlogInput = {
    name: string;
    description: string;
    websiteUrl: string;
};

export type BlogView = {
    id: string;
    name: string;
    description: string;
    websiteUrl: string;
};